# Review prompt templates — one module per review domain.
# Each module exposes a build_*_prompt(files_text: str) -> str function.
# To update review rules, edit the relevant module here — no node code needs to change.
